Imports Microsoft.VisualBasic

Public Module VariablesGlobales
    Public idUsuario As Integer
    Public nombreUsuario As String
    Public idPaciente As Integer
    Public nombrePaciente As Integer
    Public permiso As Integer
    Public cnx As String 'Hay que elegir el valor de ésto cuándo hagamos la sintaxis y un usuario, de tal manera que el mismo string funcione en tu pc y en la mía para que al hacer el ejecutable todo funcione igual.
    'Accedemos a éstas variables llamandolas desde cualquier lugar. Cambia sus valores al cada que ocupes DE MANERA DINÁMICA.
End Module
